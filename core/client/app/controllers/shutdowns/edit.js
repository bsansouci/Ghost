import Ember from 'ember';
import EditorControllerMixin from 'ghost/mixins/editor-base-controller';

var ShutdownsEditController = Ember.Controller.extend(EditorControllerMixin);

export default ShutdownsEditController;
